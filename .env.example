# Banco de dados Supabase (PostgreSQL)
DATABASE_URL=postgresql://usuario:senha@host:5432/database
DIRECT_URL=postgresql://usuario:senha@host:5432/database

# Supabase — frontend
VITE_SUPABASE_URL=https://qftxhrxzlouqvsaknwll.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_...

# JWT — use no mínimo 64 caracteres aleatórios
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=SUBSTITUA_POR_STRING_ALEATORIA_DE_64_CHARS

# API externa (opcional)
GEMINI_API_KEY=sua-chave-aqui

# CORS (opcional em produção)
ALLOWED_ORIGIN=https://seudominio.com
